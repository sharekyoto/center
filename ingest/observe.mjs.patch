--- repo-patch/observe.mjs.orig	2026-09-06 07:52:00.080196197 +0000
+++ repo-patch/observe.mjs	2026-09-06 07:52:00.090739094 +0000
@@ -51,7 +51,7 @@
   NO_RELAY_RE: /#再掲不可|#norelay\b/,
 };
 
-const NUM_RE = /#観測者(\d{4})\b/;
+const NUM_RE = /#観測員(\d{4})\b/;   // 旧 #観測者 も拾う場合は /#観測[者員](\d{4})\b/
 const today  = () => new Date().toISOString().slice(0, 10);
 
 /* ------------------------------------------------------------------ state -- */
